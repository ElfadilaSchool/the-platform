-- Attendance & Salaries Module Schema Additions
-- No table changes required because dataNewLast.sql already contains:
--  uploads, attendance_punches, attendance_exceptions, attendance_overrides,
--  attendance_settings, audit_logs, salary_raises, salary_payments
-- This migration is a no-op. Keep for audit.

BEGIN;
-- no changes
COMMIT;
